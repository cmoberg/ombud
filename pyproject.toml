[project]
name = "ombud"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.12",
    "boto3>=1.34",
    "pyyaml>=6.0",
    "mangum>=0.17",
    "starlette>=0.40",
]

[dependency-groups]
dev = [
    "uvicorn>=0.30",
    "httpx>=0.27",
    "pytest>=8.3",
]
